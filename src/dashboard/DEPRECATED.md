# Deprecated (v1)

The Streamlit dashboard is deprecated in HFI v2.

- Active UI: Next.js Content Studio in `/frontend`
- API gateway: FastAPI in `/src/api`
- Archived snapshot: `/archive/dashboard-v1`

The `src/dashboard` package is retained temporarily for backward compatibility and legacy tests.
