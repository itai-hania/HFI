hfi-prod.israelcentral.cloudapp.azure.com {
    encode gzip zstd

    @health path /health
    handle @health {
        reverse_proxy api:8000
    }

    @api path /api*
    handle @api {
        reverse_proxy api:8000
    }

    handle {
        reverse_proxy frontend:3000
    }
}
